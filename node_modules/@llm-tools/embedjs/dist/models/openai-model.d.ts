import { ClientOptions } from '@langchain/openai';
import { BaseModel } from '../interfaces/base-model.js';
import { Chunk, Message, ModelResponse } from '../global/types.js';
export declare class OpenAi extends BaseModel {
    private readonly debug;
    private readonly configuration;
    private readonly modelName;
    private model;
    constructor({ modelName, temperature, configuration, }: {
        modelName: string;
        temperature?: number;
        configuration?: ClientOptions;
    });
    init(): Promise<void>;
    runQuery(system: string, userQuery: string, supportingContext: Chunk[], pastConversations: Message[]): Promise<ModelResponse>;
}
