import { OllamaInput } from '@langchain/community/llms/ollama';
import { BaseEmbeddings } from '../interfaces/base-embeddings.js';
export declare class OllamaEmbeddings implements BaseEmbeddings {
    private model;
    constructor(options: {
        model: string;
        baseUrl: string;
        /** Defaults to "5m" */
        keepAlive?: string;
        headers?: Record<string, string>;
        requestOptions?: Omit<OllamaInput, 'baseUrl' | 'model' | 'format' | 'headers'>;
    });
    getDimensions(): Promise<number>;
    embedDocuments(texts: string[]): Promise<number[][]>;
    embedQuery(text: string): Promise<number[]>;
}
